<Window x:Class="SqlServerWorkspace.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SqlServerWorkspace"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="SQL Server Workspace" Height="800" Width="1200">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="200"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TreeView x:Name="DatabaseTreeView" SelectedItemChanged="DatabaseTreeView_SelectedItemChanged">
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                    <TextBlock Text="{Binding Name}" Style="{StaticResource TreeViewText}"/>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
            <TreeView.ItemContainerStyle>
                <Style TargetType="TreeViewItem">
                    <EventSetter Event="Loaded" Handler="DatabaseTreeViewItem_Loaded"/>
                    <EventSetter Event="Selected" Handler="DatabaseTreeViewItem_Selected"/>
                </Style>
            </TreeView.ItemContainerStyle>
        </TreeView>
        <GridSplitter Grid.Column="1" Width="3" HorizontalAlignment="Center"/>
        <Grid Grid.Column="2">
            <!--<Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>-->
            <TabControl x:Name="ContentsTabControl">
            </TabControl>
            <!--<Grid Grid.Row="1" x:Name="contentgrid">
                <Path Width="48" Height="48" Stroke="Purple" StrokeThickness="2">
                    <Path.Data>
                        M 12 0 L 24 6 L 24 18 L 12 24 L 0 18 L 0 6 Z
                        M 0 6 L 12 12 L 24 6
                        M 12 15 L 12 24 Z
                    </Path.Data>
                </Path>
            </Grid>-->
        </Grid>
    </Grid>
</Window>
