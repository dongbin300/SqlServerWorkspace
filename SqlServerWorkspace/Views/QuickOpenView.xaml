<Window x:Class="SqlServerWorkspace.Views.QuickOpenView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SqlServerWorkspace.Views"
        mc:Ignorable="d"
        WindowStartupLocation="CenterOwner"
        Title="Quick Open" Height="500" Width="400" Loaded="Window_Loaded">
    <Grid Background="{StaticResource DarkBackground}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBox Grid.Row="0" Grid.Column="0" x:Name="FilterTextBox" Margin="2" KeyDown="FilterTextBox_KeyDown" TextChanged="FilterTextBox_TextChanged"/>
        <ListBox Grid.Row="1" Grid.Column="0" x:Name="ItemListBox" Style="{StaticResource DarkListBox}" SelectionChanged="ItemListBox_SelectionChanged" MouseDoubleClick="ItemListBox_MouseDoubleClick"
                 PreviewKeyDown="ItemListBox_PreviewKeyDown"
                 KeyboardNavigation.DirectionalNavigation="Cycle" KeyboardNavigation.TabNavigation="Continue">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Path 
                Data="{Binding SvgData}"
                Fill="{Binding SvgColor}"
                Width="16" Height="16" 
                Stretch="Uniform"
                Margin="0,0,5,0"
                VerticalAlignment="Center"
                HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding Name}" VerticalAlignment="Center"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
